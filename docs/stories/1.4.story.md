# Story 1.4: Transaction List & Basic Dashboard

**Epic:** 1 — Foundation & Core Infrastructure
**Status:** Ready for Review
**Complexity:** M (Medium) — 5 Story Points
**Estimated Effort:** 2-3 dias
**Author:** River (@sm)
**PRD Ref:** Story 1.4 (prd.md v1.3), FR10-FR13
**Arch Ref:** Sections 7, 2 (architecture.md v1.2)

---

## Context

Com transacoes importadas (Story 1.3), o usuario precisa visualizar seus dados. Esta story entrega a primeira experiencia real do produto: uma lista de transacoes navegavel e um dashboard basico com resumo financeiro.

Neste ponto do MVP, a categorizacao ainda e stub (`category='outros'`). O dashboard deve ser visualmente impactante MESMO SEM categorizacao — agrupando por dia/semana, mostrando receitas vs despesas, saldo. A versao com graficos de categorias vem no Epic 2.

**Decisao de produto confirmada:** O dashboard gratuito deve ser visualmente completo e bonito, nao um placeholder degradado. Este e o primeiro contato real do usuario com o valor do app.

**PRD refs:** FR10 (dashboard consolidado), FR11 (totais), FR12 (visualmente impactante no free), FR13 (lista com filtros)
**Arch refs:** Frontend Architecture (section 7), Component Strategy (section 7.1), Data Fetching (section 7.2), Mobile Layout (section 7.3)

---

## User Story

> Como usuario,
> quero ver todas as minhas transacoes importadas organizadas e um resumo do meu mes,
> para que eu tenha visibilidade de para onde vai meu dinheiro.

---

## Acceptance Criteria

### AC1: Lista de transacoes com scroll infinito
- [x] `src/app/(app)/transactions/page.tsx` — exibe lista de transacoes
- [x] Cada item mostra: data, descricao, valor (colorido: verde para credito, vermelho para debito), tipo (entrada/saida)
- [x] Scroll infinito (carregar mais ao chegar no fim — 50 transacoes por pagina)
- [x] Ordenacao por data decrescente (mais recente primeiro)
- [x] Performance: primeira carga < 1s com 500 transacoes no banco
- [ ] **Verificacao:** Importar extrato com 200+ transacoes; lista carrega rapido; scroll infinito funciona

### AC2: Filtros funcionais
- [x] Filtro por periodo: mes atual (default), mes anterior, customizado (date picker)
- [x] Filtro por tipo: todos (default), receitas, despesas
- [x] Filtros aplicados via query params (persistem em refresh)
- [x] UI: filtros em barra fixa acima da lista, design compacto para mobile
- [x] Combinacao de filtros funciona (ex: despesas do mes anterior)
- [ ] **Verificacao:** Aplicar cada filtro individualmente e combinados; resultados corretos

### AC3: Busca por texto
- [x] Campo de busca na descricao das transacoes
- [x] Busca case-insensitive, sem acentos (usar `ilike` no Supabase ou filtro client-side)
- [x] Debounce de 300ms para evitar queries excessivas
- [ ] **Verificacao:** Buscar "uber" retorna todas as transacoes com "uber" na descricao

### AC4: Dashboard basico — Cards de resumo
- [x] `src/app/(app)/dashboard/page.tsx` — Server Component
- [x] 3 cards de resumo para o periodo selecionado:
  - **Total Receitas:** soma de transacoes com `type='credit'` — cor verde
  - **Total Despesas:** soma de transacoes com `type='debit'` — cor vermelha
  - **Saldo do Periodo:** receitas - despesas — cor contextual (verde se positivo, vermelho se negativo)
- [x] Valores formatados em BRL: `R$ 1.234,56` (ponto para milhar, virgula para decimal)
- [x] Periodo default: mes atual
- [x] `src/components/dashboard/summary-cards.tsx` — componente reutilizavel
- [ ] **Verificacao:** Importar extrato; dashboard mostra totais corretos; formatacao BRL correta

### AC5: Dashboard visualmente impactante sem categorizacao
- [x] Agrupamento de transacoes por semana (nao por categoria — essa vem no Epic 2)
- [x] Grafico simples: barras de receita vs despesa por semana do mes (Recharts BarChart)
- [x] Visual limpo, cores contrastantes, tipografia clara
- [x] Deve parecer um produto completo, nao um MVP inacabado
- [ ] **Verificacao:** Screenshot do dashboard em mobile parece profissional e informativo

### AC6: Pull-to-refresh
- [x] Na lista de transacoes: botao "Atualizar" visivel com feedback visual (spinner)
- [x] Alternativa web implementada (pull-to-refresh nativo nao disponivel em web)
- [ ] **Verificacao:** Importar novo extrato em outra aba; atualizar mostra novas transacoes

### AC7: Empty state
- [x] Se nao ha transacoes: exibir estado vazio bonito com icone + mensagem + CTA
- [x] CTA primario: "Importar meu primeiro extrato" → link para `/import`
- [x] Empty state no dashboard E na lista de transacoes
- [ ] **Verificacao:** Novo usuario sem importacoes ve empty state com CTA funcional

### AC8: Responsivo e performatico em mobile
- [x] Design mobile-first: viewport 360px como referencia
- [x] Touch targets minimos 44x44px (min-h-[56px] nos items, min-w-[44px] nos tabs)
- [x] Sem scroll horizontal indesejado
- [x] Dashboard carrega em < 2s (Server Component, dados no servidor)
- [ ] **Verificacao:** Chrome DevTools Mobile (Moto G Power, Galaxy S8) — sem quebras de layout

### AC9: Bottom navigation
- [x] `src/components/layout/bottom-nav.tsx` — Navegacao fixa no rodape
- [x] 5 tabs: Home (dashboard), Transacoes, Importar (+), Chat, Config
- [x] Chat e Config desabilitados (opacity-40, aria-disabled)
- [x] Botao central "+" destacado com rounded-full + bg-primary
- [x] Tab ativa destacada (text-primary)
- [x] Integrado ao layout do `(app)` group
- [ ] **Verificacao:** Navegacao entre Dashboard e Transacoes funciona; tabs inativas mostram estado disabled

### AC10: Formatacao PT-BR
- [x] `src/lib/utils/format.ts` com funcoes: formatCurrency, formatDate, formatDateShort, formatDateRelative, getMonthBounds, getPrevMonthBounds
- [x] Usado consistentemente em dashboard e lista de transacoes
- [ ] **Verificacao:** Valores e datas exibidos no formato brasileiro correto

### AC11: Hook useTransactions
- [x] `src/hooks/use-transactions.ts` implementado
- [x] Suporte a filtros: startDate, endDate, type, search
- [x] Usa Supabase browser client com RLS
- [x] Paginacao offset/limit para scroll infinito (PAGE_SIZE=50)
- [ ] **Verificacao:** Hook retorna dados corretos com filtros aplicados

---

## Dependencies

- **Blocked by:** Story 1.1 (projeto, schema), Story 1.2 (auth, layout do app), Story 1.3 (transacoes importadas)
- **Blocks:** Epic 2 (dashboard com categorias estende este dashboard basico)

---

## Tech Notes for @dev

### Data Fetching Strategy
- **Dashboard (`/dashboard`):** Server Component. Fetch dados no servidor com Supabase server client. Rapido, SEO-friendly (nao relevante aqui, mas e o padrao correto).
- **Transaction List (`/transactions`):** Pode ser Server Component com filtros via search params, ou Client Component com hooks. Recomendacao: Client Component para interatividade de filtros + scroll infinito.

### Queries Supabase (exemplos)
```typescript
// Summary para dashboard
const { data } = await supabase
  .from('transactions')
  .select('amount, type')
  .gte('date', startOfMonth)
  .lte('date', endOfMonth)

const income = data.filter(t => t.type === 'credit').reduce((sum, t) => sum + t.amount, 0)
const expenses = data.filter(t => t.type === 'debit').reduce((sum, t) => sum + Math.abs(t.amount), 0)

// Lista com paginacao
const { data, count } = await supabase
  .from('transactions')
  .select('*', { count: 'exact' })
  .order('date', { ascending: false })
  .range(offset, offset + limit - 1)
```

### Scroll Infinito
- Usar `IntersectionObserver` para detectar quando o usuario chega ao fim da lista
- Carregar proxima pagina (50 itens) e append na lista
- Mostrar spinner enquanto carrega
- Parar quando `count` total for atingido

### Graficos
- **Recharts** e a melhor opcao: leve, React-native, responsivo
- Para MVP: um unico grafico de barras (receita vs despesa por semana)
- Nao usar Chart.js (mais pesado, menos integracao com React)

### Formatacao BRL
```typescript
// src/lib/utils/format.ts
export function formatCurrency(amount: number): string {
  return new Intl.NumberFormat('pt-BR', {
    style: 'currency',
    currency: 'BRL'
  }).format(amount)
}

export function formatDate(date: string): string {
  return new Intl.DateTimeFormat('pt-BR', {
    day: '2-digit',
    month: 'short',
    year: 'numeric'
  }).format(new Date(date))
}
```

### Mobile Layout
- Bottom nav: `fixed bottom-0` com `z-50`, `h-16`
- Page content: `pb-16` para compensar bottom nav
- Cards de resumo: `grid grid-cols-1 sm:grid-cols-3` — empilhados em mobile, lado a lado em desktop
- Lista: `divide-y` com items padded para touch

### shadcn/ui Components (instalar)
- `Card` — summary cards
- `Input` — campo de busca
- `Select` — filtro de periodo/tipo
- `Button` — CTAs
- `Skeleton` — loading states
- `Badge` — tipo de transacao

### Empty State
- Pode usar icone do Lucide Icons (ja incluso com shadcn)
- Ex: `<FileX2 />` ou `<Inbox />` com texto e botao

---

## Out of Scope

- Graficos por categoria (Epic 2 — requer categorizacao real)
- Comparativo mes-a-mes (Epic 2/3 — Premium)
- Edicao de transacoes / correcao de categoria (Epic 2)
- Chat IA e Settings na bottom nav (ficam disabled, implementados depois)
- Export de dados (futuro)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/app/(app)/dashboard/page.tsx` | ✅ Updated | Dashboard principal (Server Component) |
| `src/app/(app)/transactions/page.tsx` | ✅ Created | Lista de transacoes com filtros + busca |
| `src/components/dashboard/summary-cards.tsx` | ✅ Created | Cards receita/despesa/saldo |
| `src/components/dashboard/spending-chart.tsx` | ✅ Created | BarChart receita vs despesa por semana (Recharts) |
| `src/components/transactions/transaction-list.tsx` | ✅ Created | Lista com scroll infinito + empty state |
| `src/components/transactions/transaction-item.tsx` | ✅ Created | Item individual (data, descricao, valor, categoria) |
| `src/components/layout/bottom-nav.tsx` | ✅ Created | Bottom navigation (5 tabs, 2 disabled) |
| `src/hooks/use-transactions.ts` | ✅ Created | Hook de transacoes + filtros + paginacao |
| `src/lib/utils/format.ts` | ✅ Created | Formatacao BRL + datas PT-BR |
| `src/app/(app)/layout.tsx` | ✅ Updated | Integrado bottom-nav + pb-16 |
| `src/lib/supabase/types.ts` | ✅ Updated | Adicionado Tables/TablesInsert/TablesUpdate helpers |
| `src/components/ui/card.tsx` | ✅ Added | shadcn Card component |
| `src/components/ui/input.tsx` | ✅ Added | shadcn Input component |
| `src/components/ui/select.tsx` | ✅ Added | shadcn Select component |
| `src/components/ui/skeleton.tsx` | ✅ Added | shadcn Skeleton component |
| `src/components/ui/badge.tsx` | ✅ Added | shadcn Badge component |
| `package.json` | ✅ Updated | Adicionado recharts |
