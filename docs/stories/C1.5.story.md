# Story C1.5: Detecção de Cobranças Duplicadas

**Epic:** C1 — Ciclo 1: MVP Extensions
**Status:** Draft
**Complexity:** S (Small) — 2 Story Points
**Estimated Effort:** 2 dias
**Author:** River (@sm)
**PRD Ref:** docs/market-research-features.md (Ciclo 1, "Detecção de gastos duplicados / Alerta de cobrança indevida")
**Arch Ref:** docs/feature-inventory.md ("Detecção de cobranças duplicadas: ⬜ não existe")

---

## Context

O SHA-256 já existe para deduplicação **na importação** — o `external_id` único por conta evita que a mesma transação do banco seja importada duas vezes. Mas isso não detecta quando o _usuário realmente foi cobrado duas vezes_ pelo mesmo serviço.

Rocket Money (internacional) e Copilot Money têm detecção de cobranças duplicadas como feature de destaque. É um dos itens mais elogiados em reviews: "economizei R$300/mês descobrindo cobranças indevidas".

O caso real: mesma assinatura cobrada duas vezes no mesmo mês, uma cobrança de supermercado duplicada (erro do comerciante), ou cobrança em dois cartões diferentes. A lógica é simples: mesma descrição normalizada + mesmo valor + datas próximas (janela de 3 dias).

O usuário pode dispensar o alerta marcando como "não é duplicata" — o que esconde permanentemente aquele par de transações dos alertas futuros.

---

## User Story

> Como usuário,
> quero ser alertado quando o app detectar possível cobrança duplicada,
> para identificar cobranças indevidas e agir rapidamente,
> sem precisar verificar manualmente cada transação.

---

## Acceptance Criteria

### AC1: Migration — tabela `duplicate_alerts`

- [ ] Nova migration `018_duplicate_alerts.sql` em `supabase/migrations/`
- [ ] Schema:
  ```sql
  CREATE TABLE duplicate_alerts (
    id               uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id          uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    transaction_id_1 uuid NOT NULL REFERENCES transactions(id) ON DELETE CASCADE,
    transaction_id_2 uuid NOT NULL REFERENCES transactions(id) ON DELETE CASCADE,
    status           text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'dismissed')),
    created_at       timestamptz DEFAULT now(),
    CONSTRAINT duplicate_alerts_unique UNIQUE(transaction_id_1, transaction_id_2)
  );
  ```
- [ ] Garantir que `transaction_id_1 < transaction_id_2` (ordering para evitar duplicatas invertidas) — enforçar via trigger ou convention
- [ ] RLS: `user_id = auth.uid()` para SELECT, UPDATE (para dismiss), DELETE
- [ ] Índice em `(user_id, status)` para buscar alertas pendentes no dashboard
- [ ] **Verificação:** `supabase db push` sem erros

### AC2: Lógica de detecção de duplicatas

- [ ] Implementar função `detectDuplicates(userId: string, importedTransactionIds: string[])` em `src/lib/transactions/duplicate-detector.ts`
- [ ] Algoritmo: para cada transação nova importada, buscar transações do mesmo usuário onde:
  - `description_normalized` é idêntico (normalizar: uppercase, remover pontuação, espaços extras)
  - `amount` é idêntico
  - `date` está dentro de uma janela de ±3 dias
  - `type` é idêntico (ambas `debit`)
  - A outra transação **não é a própria** (ids diferentes)
- [ ] Normalização de descrição: `description.toUpperCase().replace(/[^A-Z0-9 ]/g, '').replace(/\s+/g, ' ').trim()`
- [ ] Para cada par detectado: inserir em `duplicate_alerts` (ON CONFLICT DO NOTHING — idempotente)
- [ ] Função retorna o número de novos alertas criados
- [ ] **Verificação:** Testes unitários em `tests/unit/transactions/duplicate-detector.test.ts` cobrem: detecção correta, janela de 3 dias, não detectar em créditos, par já existente (idempotência)

### AC3: Integrar detecção ao fluxo de importação

- [ ] `POST /api/import` (em `src/app/api/import/route.ts`) chama `detectDuplicates()` após salvar as transações
- [ ] Chamada em background (não bloqueia a resposta de importação)
- [ ] Se detecção falhar: logar erro, não propagar para o usuário
- [ ] **Verificação:** Importar extrato com transação duplicada real → alerta criado em `duplicate_alerts`

### AC4: Card de alerta no dashboard

- [ ] Novo componente `DuplicateAlertsCard` em `src/components/dashboard/duplicate-alerts-card.tsx`
- [ ] Exibido no dashboard **somente quando existem alertas com `status = 'pending'`**
- [ ] Visual: card com borda amarela/laranja, ícone de atenção
- [ ] Conteúdo:
  - Título: "Possível cobrança duplicada"
  - Para cada par de alerta: exibir as duas transações (data, descrição, valor)
  - Botão "Não é duplicata" por par — chama PATCH para `status = 'dismissed'`
  - Se > 3 alertas: exibir os 3 primeiros + "Ver todos (N)"
- [ ] Após dispensar todos os alertas: card desaparece automaticamente
- [ ] **Verificação:** Com alertas pendentes → card visível no dashboard; clicar "Não é duplicata" → par desaparece do card

### AC5: API para gerenciar alertas

- [ ] `GET /api/duplicate-alerts` — retorna alertas `pending` do usuário logado, com JOIN nas duas transações
- [ ] `PATCH /api/duplicate-alerts/[id]` — atualiza status para `dismissed`
  - Body: `{ "status": "dismissed" }`
  - Verifica que o alerta pertence ao usuário logado
- [ ] **Verificação:** GET retorna alertas pendentes; PATCH dismiss → alerta some do GET

---

## Dependencies

- **Blocked by:** Nenhuma
- **Blocks:** Nenhuma

---

## Tech Notes for @dev

### Normalização de descrição (consistente com SHA-256 existente)

```typescript
// src/lib/transactions/duplicate-detector.ts
function normalizeDescription(desc: string): string {
  return desc
    .toUpperCase()
    .replace(/[^A-Z0-9 ]/g, '')
    .replace(/\s+/g, ' ')
    .trim();
}
```

### Algoritmo de detecção (SQL direto no Supabase client)

```typescript
export async function detectDuplicates(
  supabase: SupabaseClient,
  userId: string,
  newTransactionIds: string[]
): Promise<number> {
  // Buscar as novas transações
  const { data: newTxns } = await supabase
    .from('transactions')
    .select('id, description, amount, type, date')
    .in('id', newTransactionIds)
    .eq('type', 'debit');

  let alertsCreated = 0;

  for (const txn of newTxns ?? []) {
    const normalized = normalizeDescription(txn.description);
    const dateObj = new Date(txn.date);
    const dateMinus3 = new Date(dateObj);
    dateMinus3.setDate(dateMinus3.getDate() - 3);
    const datePlus3 = new Date(dateObj);
    datePlus3.setDate(datePlus3.getDate() + 3);

    const { data: candidates } = await supabase
      .from('transactions')
      .select('id, description, amount, date')
      .eq('user_id', userId)
      .eq('type', 'debit')
      .eq('amount', txn.amount)
      .neq('id', txn.id)
      .gte('date', dateMinus3.toISOString().slice(0, 10))
      .lte('date', datePlus3.toISOString().slice(0, 10));

    for (const candidate of candidates ?? []) {
      if (normalizeDescription(candidate.description) !== normalized) continue;

      // Ordenar IDs para garantir unicidade do par
      const [id1, id2] = [txn.id, candidate.id].sort();

      const { error } = await supabase.from('duplicate_alerts').insert({
        user_id: userId,
        transaction_id_1: id1,
        transaction_id_2: id2,
        status: 'pending',
      });

      if (!error) alertsCreated++;
    }
  }

  return alertsCreated;
}
```

### Posicionamento do card no dashboard

O `DuplicateAlertsCard` deve ser posicionado abaixo dos `SummaryCards` mas acima dos gráficos — área de "alertas e ações necessárias" do dashboard.

### Query do dashboard (server component)

```typescript
// Em dashboard/page.tsx, adicionar ao fetch paralelo:
const { data: duplicateAlerts } = await supabase
  .from('duplicate_alerts')
  .select(`
    id, status,
    t1:transaction_id_1(id, date, description, amount),
    t2:transaction_id_2(id, date, description, amount)
  `)
  .eq('user_id', user.id)
  .eq('status', 'pending')
  .limit(5);
```

---

## Out of Scope

- Detecção em transações já existentes (histórico) — apenas nas novas importações
- Detecção de cobranças em valores ligeiramente diferentes (ex: R$10,00 vs R$10,01)
- Notificação push para duplicatas (pode ser adicionada em ciclos futuros)
- Cancelamento automático de cobranças duplicadas
- Detecção cross-account (entre contas bancárias diferentes do mesmo usuário)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `supabase/migrations/018_duplicate_alerts.sql` | Created | Tabela duplicate_alerts + RLS + índices |
| `src/lib/supabase/types.ts` | Updated | Regenerado após migration |
| `src/lib/transactions/duplicate-detector.ts` | Created | Função detectDuplicates com algoritmo |
| `src/app/api/import/route.ts` | Updated | Chamar detectDuplicates em background após import |
| `src/app/api/duplicate-alerts/route.ts` | Created | GET — listar alertas pendentes |
| `src/app/api/duplicate-alerts/[id]/route.ts` | Created | PATCH — dismiss alerta |
| `src/components/dashboard/duplicate-alerts-card.tsx` | Created | Card de alertas no dashboard |
| `src/app/(app)/dashboard/page.tsx` | Updated | Incluir DuplicateAlertsCard quando houver alertas |
| `tests/unit/transactions/duplicate-detector.test.ts` | Created | Testes unitários do detector |

---

## QA Results

_A preencher pelo @qa_

---

## Dev Agent Record

### Agent Model Used
_A preencher pelo @dev_

### Completion Notes
_A preencher pelo @dev_

### Change Log
| Date | Agent | Change |
|------|-------|--------|
| 2026-04-10 | River (@sm) | Story criada para Epic C1 |
