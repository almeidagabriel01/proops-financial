# Story 2.2: User Category Correction & Learning

**Epic:** 2 — IA: Categorizacao Inteligente
**Status:** Ready for Review
**Complexity:** M (Medium) — 5 Story Points
**Estimated Effort:** 2-3 dias
**Author:** River (@sm)
**PRD Ref:** Story 2.2 (prd.md v1.3), FR8, FR9
**Arch Ref:** Sections 3.1 (`category_dictionary`), 5.1 (tier hierarchy) (architecture.md v1.0)

---

## Context

A IA categoriza bem, mas nao e perfeita. Esta story fecha o loop de aprendizado: o usuario corrige categorizacoes erradas, e o sistema aprende para nunca repetir o mesmo erro. O aprendizado e persistido na tabela `category_dictionary` por usuario.

A hierarquia de categorizacao (arch section 5.1) ja preve esta camada:
1. **Dicionario do usuario** (`category_dictionary`) — esta story popula esta camada
2. Cache global (`category_cache`) — Story 2.1
3. Claude Haiku — Story 2.1

Apos uma correcao, todas as transacoes futuras com a mesma descricao normalizada usam o dicionario do usuario — sem chamar a IA. Transacoes passadas com a mesma descricao devem ser atualizadas em batch opcional ("corrigir todas").

**PRD refs:** FR8 (corrigir + aprender), FR9 (dicionario que melhora com uso)
**Arch refs:** `category_dictionary` table (section 3.1), Tier 1 hierarchy (section 5.1)

---

## User Story

> Como usuario,
> quero poder corrigir a categoria de uma transacao,
> para que o app aprenda minhas preferencias e melhore ao longo do tempo.

---

## Acceptance Criteria

### AC1: Detalhe da transacao com opcao de corrigir categoria
- [x] Na lista de transacoes (`/transactions`), tap/click em uma transacao abre painel de detalhe (Sheet ou Dialog do shadcn/ui)
- [x] Painel exibe: data, descricao completa, valor formatado em BRL, categoria atual com icone/cor
- [x] Botao "Corrigir categoria" visivel no painel
- [x] **Verificacao:** Tap em qualquer transacao; painel abre com dados corretos; botao de correcao visivel

### AC2: Selector de categoria
- [x] `src/components/transactions/category-selector.tsx` — componente reutilizavel
- [x] Exibe as 14 categorias fixas em grid com icone + nome (PT-BR)
- [x] Categoria atual destacada visualmente (cor diferente / borda)
- [x] Icones por categoria (Lucide Icons): alimentacao=`Utensils`, delivery=`Package`, transporte=`Car`, moradia=`Home`, saude=`Heart`, educacao=`BookOpen`, lazer=`Music`, compras=`ShoppingBag`, assinaturas=`Repeat`, transferencias=`ArrowLeftRight`, salario=`Banknote`, investimentos=`TrendingUp`, impostos=`Receipt`, outros=`MoreHorizontal`
- [x] **Verificacao:** Selector exibe 14 categorias com icones; categoria atual esta destacada

### AC3: Salvar correcao e atualizar dicionario
- [x] Ao selecionar nova categoria: `UPDATE transactions SET category=$1, category_source='user', updated_at=now() WHERE id=$2`
- [x] Upsert em `category_dictionary`: `INSERT INTO category_dictionary (user_id, description_pattern, category) VALUES ($1, normalize_description($description), $3) ON CONFLICT (user_id, description_pattern) DO UPDATE SET category=$3, usage_count=usage_count+1, updated_at=now()`
- [x] Feedback visual imediato: categoria atualizada na lista sem reload de pagina
- [x] Painel fecha apos salvar com sucesso
- [x] Tratamento de erro: mensagem clara em PT-BR se falhar
- [x] **Verificacao:** Corrigir categoria de uma transacao; dicionario atualizado no banco; lista mostra nova categoria imediatamente

### AC4: Correcao em batch — "corrigir todas"
- [x] Apos selecionar nova categoria, mostrar opcao: "Corrigir so esta" vs "Corrigir todas com a mesma descricao (N transacoes)"
- [x] "Corrigir todas": `UPDATE transactions SET category=$1, category_source='user' WHERE user_id=$2 AND id = ANY($ids_com_mesma_descricao)`
- [x] Calcular `ids_com_mesma_descricao` via `normalize_description` antes de mostrar a opcao
- [x] Se houver apenas 1 transacao com aquela descricao: nao mostrar a opcao batch (simplificacao UX)
- [x] **Verificacao:** Extrato com 5 transacoes "UBER": corrigir uma com "Corrigir todas"; todas as 5 mudam de categoria

### AC5: Hierarquia de categorizacao respeitada em novas importacoes
- [x] A Edge Function `categorize-import` (Story 2.1 AC3) JA implementa Tier 1 (dicionario do usuario)
- [x] Verificar que nova importacao com descricao ja corrigida usa `category_source='user'` e nao chama a IA
- [x] **Verificacao:** Corrigir "UBER" para `transporte`; importar novo extrato com transacoes "UBER"; todas tem `category_source='user'` sem chamar Haiku

### AC6: Persistencia no banco com RLS
- [x] Correcao de categoria: usuario so pode atualizar suas proprias transacoes (RLS garante)
- [x] `category_dictionary`: usuario so pode CRUD seu proprio dicionario (RLS conforme schema)
- [x] Nao usar service role para estas operacoes — usar browser client com RLS
- [x] **Verificacao:** Tentar atualizar transacao de outro usuario via API; deve retornar 0 rows updated

### AC7: Testes unitarios
- [x] `tests/unit/ai/category-correction.test.ts` — testar logica de normalizacao e upsert no dicionario
- [x] Testar batch correction: N transacoes com mesma descricao sao todas atualizadas
- [x] Testar que correcao unica nao afeta outras transacoes
- [x] **Verificacao:** `npm test` passa sem erros

---

## Dependencies

- **Blocked by:** Story 1.4 (lista de transacoes onde correcao acontece), Story 2.1 (Edge Function le o dicionario; esta story o popula)
- **Blocks:** Story 2.3 (dashboard de categorias exibe dados mais precisos apos correcoes)

---

## Tech Notes for @dev

### Normalizacao de descricao (client-side)
```typescript
// src/lib/utils/format.ts — adicionar funcao:
export function normalizeDescription(raw: string): string {
  return raw
    .toLowerCase()
    .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9 ]/g, '')
    .replace(/\s+/g, ' ')
    .trim()
}
// IMPORTANTE: deve ser identica a public.normalize_description() SQL
```

### Upsert no dicionario via Supabase client
```typescript
const supabase = createClientBrowser() // client-side, RLS aplicado
await supabase
  .from('category_dictionary')
  .upsert({
    user_id: userId,
    description_pattern: normalizeDescription(transaction.description),
    category: newCategory,
    usage_count: 1
  }, { onConflict: 'user_id,description_pattern' })
```

### Buscar transacoes com mesma descricao normalizada
```typescript
const pattern = normalizeDescription(transaction.description)
const { data: sameDesc } = await supabase
  .from('transactions')
  .select('id')
  .eq('user_id', userId)
  .neq('id', transaction.id) // excluir a atual
  // Filtragem client-side apos busca ou via RPC se performance exigir
```
> Alternativa mais performatica: criar RPC `get_transactions_by_pattern(user_id, pattern)` se a query client-side for lenta com muitas transacoes.

### shadcn/ui Components necessarios
- `Sheet` (bottom sheet em mobile, lateral em desktop) — para painel de detalhe
- `Dialog` — alternativa ao Sheet se preferivel
- Estes componentes podem precisar ser instalados: `npx shadcn@latest add sheet dialog`

### Icones das categorias (Lucide)
```typescript
// src/lib/utils/categories.ts — constante central:
export const CATEGORY_CONFIG = {
  alimentacao:    { icon: 'Utensils',        label: 'Alimentacao',   color: '#22c55e' },
  delivery:       { icon: 'Package',         label: 'Delivery',      color: '#f97316' },
  transporte:     { icon: 'Car',             label: 'Transporte',    color: '#3b82f6' },
  moradia:        { icon: 'Home',            label: 'Moradia',       color: '#8b5cf6' },
  saude:          { icon: 'Heart',           label: 'Saude',         color: '#ec4899' },
  educacao:       { icon: 'BookOpen',        label: 'Educacao',      color: '#14b8a6' },
  lazer:          { icon: 'Music',           label: 'Lazer',         color: '#eab308' },
  compras:        { icon: 'ShoppingBag',     label: 'Compras',       color: '#f43f5e' },
  assinaturas:    { icon: 'Repeat',          label: 'Assinaturas',   color: '#6366f1' },
  transferencias: { icon: 'ArrowLeftRight',  label: 'Transferencias',color: '#64748b' },
  salario:        { icon: 'Banknote',        label: 'Salario',       color: '#10b981' },
  investimentos:  { icon: 'TrendingUp',      label: 'Investimentos', color: '#0ea5e9' },
  impostos:       { icon: 'Receipt',         label: 'Impostos',      color: '#78716c' },
  outros:         { icon: 'MoreHorizontal',  label: 'Outros',        color: '#9ca3af' },
} as const
// NOTA: Este arquivo e SHARED entre Story 2.2 e 2.3
```

### Otimistic UI
- Ao salvar correcao: atualizar o estado local imediatamente antes de confirmar no banco
- Se a query falhar: reverter para categoria original com mensagem de erro
- Isso evita o "flash" de categoria antiga enquanto a query retorna

---

## Out of Scope

- Criacao de categorias customizadas pelo usuario (fora do escopo do MVP)
- Correcao em massa via bulk select na lista (futuro)
- Exportar/importar dicionario de categorias (futuro)
- Ver ou gerenciar o dicionario completo de regras (futuro — tela de configuracoes v2)
- Desfazer correcao (nao previsto no PRD)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/components/transactions/category-selector.tsx` | To Create | Grid de 14 categorias com icone + label |
| `src/components/transactions/transaction-detail.tsx` | To Create | Sheet/Dialog com detalhe + correcao |
| `src/lib/utils/categories.ts` | To Create | CATEGORY_CONFIG: icones, labels, cores por categoria |
| `src/app/(app)/transactions/page.tsx` | To Update | Integrar transaction-detail ao clicar em item |
| `src/lib/utils/format.ts` | To Update | Adicionar normalizeDescription() |
| `tests/unit/ai/category-correction.test.ts` | To Create | Testes de correcao e batch update |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log
Nenhum bloqueador encontrado.

### Completion Notes
- `transaction-item.tsx` transformado em Client Component com estado local `localCategory` para UI otimista
- Modo `saving` no Sheet serve tanto para busca de duplicatas quanto para persistência
- `useEffect` para sincronização de categoria removido após lint — atualização otimista é suficiente para o MVP
- `normalizeDescription` em `format.ts` é a fonte de verdade compartilhada com o SQL

### File List
| File | Status | Description |
|------|--------|-------------|
| `src/components/transactions/category-selector.tsx` | Created | Grid 3/4 colunas com 14 categorias, ícones Lucide, destaque visual na atual |
| `src/components/transactions/transaction-detail.tsx` | Created | Sheet bottom com 4 modos: view / selecting / confirming / saving |
| `src/components/transactions/transaction-item.tsx` | Updated | Área principal clicável abre TransactionDetail; estado local localCategory |
| `src/lib/utils/categories.ts` | Created | CATEGORY_CONFIG: ícones LucideIcon, labels PT-BR, cores hex para 14 categorias |
| `src/lib/utils/format.ts` | Updated | Adicionado normalizeDescription() — espelho de normalize_description() SQL |
| `src/lib/ai/category-correction.ts` | Created | saveCorrection() + findSameDescriptionIds() — browser client, RLS aplicado |
| `tests/unit/ai/category-correction.test.ts` | Created | 16 testes: normalizeDescription (6), saveCorrection (5), findSameDescriptionIds (4) |

### Change Log
| Date | Agent | Change |
|------|-------|--------|
| 2026-04-05 | River (@sm) | Story criada para Epic 2 |
| 2026-04-07 | Dex (@dev) | Implementação completa — todos ACs; 102 testes passando; lint + build OK |
