# Story 1.1: Project Bootstrap & DevOps Setup

**Epic:** 1 — Foundation & Core Infrastructure
**Status:** InProgress
**Complexity:** M (Medium) — 5 Story Points
**Estimated Effort:** 1-2 dias
**Author:** River (@sm)
**PRD Ref:** Story 1.1 (prd.md v1.3)
**Arch Ref:** Sections 2, 3, 9.2, 10 (architecture.md v1.2)

---

## Context

Esta e a primeira story do projeto. Nada existe ainda — nenhum codigo, nenhum repositorio, nenhum banco de dados. O objetivo e criar toda a fundacao tecnica: projeto Next.js com App Router, conexao com Supabase (PostgreSQL + Auth + Storage), configuracao de ambiente, tooling de qualidade de codigo, e um health check funcional.

Tudo que vier depois (auth, import, dashboard, IA) depende desta story estar 100% correta. Erros aqui se propagam para todo o projeto.

**PRD refs:** FR20 (Supabase Auth setup), NFR8-10 (infra), NFR12 (custo), CON1-2 (dev solo, budget)
**Arch refs:** Project Structure (section 2), Database Schema (section 3), Env Vars (section 9.2), Deployment (section 10)

---

## User Story

> Como desenvolvedor,
> quero ter o projeto Next.js inicializado com frontend, backend, banco de dados e CI basico,
> para que eu tenha uma base solida para desenvolver todas as features seguintes.

---

## Acceptance Criteria

### AC1: Projeto Next.js inicializado
- [x] `npx create-next-app@latest` com TypeScript, App Router, Tailwind CSS, `src/` directory, import alias `@/`
- [x] Estrutura de pastas conforme arquitetura: `src/app/(auth)/`, `src/app/(app)/`, `src/app/api/`, `src/components/`, `src/lib/`, `src/hooks/`
- [x] `next.config.ts` configurado
- [x] **Verificacao:** `npm run dev` inicia sem erros, pagina renderiza em `localhost:3000`

### AC2: Supabase projeto criado e conectado
- [x] Projeto Supabase criado (free tier)
- [x] Supabase CLI instalado e `supabase init` executado no projeto
- [x] `supabase/config.toml` configurado
- [ ] **Verificacao:** `supabase status` retorna conexao valida ao projeto local

### AC3: Schema do banco de dados aplicado
- [x] Migration `001_initial_schema.sql` criada em `supabase/migrations/` com TODAS as 8 tabelas do architecture.md:
  - `profiles`, `bank_accounts`, `imports`, `transactions`, `category_dictionary`, `category_cache`, `chat_messages`, `subscriptions`
- [x] Todos os indexes criados (7 indexes conforme schema)
- [x] Todas as check constraints aplicadas (category enum em 3 tabelas, status checks, type checks)
- [x] Funcoes SQL criadas: `update_updated_at()`, `sync_plan_from_subscription()`, `normalize_description()`, `handle_new_user()`
- [x] Todos os triggers criados (5x `updated_at`, 1x `sync_plan`, 1x `on_auth_user_created`)
- [x] RLS habilitado em TODAS as 8 tabelas com policies conforme architecture.md section 3.2
- [x] Storage bucket `imports` criado com RLS policies
- [ ] **Verificacao:** `supabase db push` executa sem erros; tabelas visiveis no Supabase Dashboard

### AC4: Health-check API route
- [x] `src/app/api/health/route.ts` implementado (GET)
- [x] Retorna JSON: `{ status: 'ok', supabase: true/false, timestamp: ISO8601 }`
- [x] Testa conexao real ao Supabase (query simples)
- [x] Sem autenticacao (endpoint publico)
- [ ] **Verificacao:** `curl localhost:3000/api/health` retorna 200 com `supabase: true`

### AC5: Pagina inicial placeholder
- [x] `src/app/page.tsx` renderiza pagina placeholder com nome do app
- [x] Responsivo em mobile (viewport meta tag, mobile-first CSS)
- [ ] **Verificacao:** Pagina carrega no Chrome DevTools em modo mobile sem erros de console

### AC6: Git inicializado
- [x] `.gitignore` adequado (node_modules, .env.local, .next, .supabase, etc.)
- [ ] Primeiro commit feito com todos os arquivos de setup
- [ ] **Verificacao:** `git log` mostra pelo menos 1 commit; `git status` esta limpo

### AC7: Linter e formatter configurados
- [x] ESLint configurado com regras Next.js + TypeScript
- [x] Prettier configurado com regras consistentes (semi, singleQuote, trailingComma)
- [x] Scripts no `package.json`: `lint`, `lint:fix`, `format`
- [x] **Verificacao:** `npm run lint` executa sem erros no codigo inicial

### AC8: Dev server funcional
- [ ] `npm run dev` com hot reload funcionando
- [ ] Alteracoes em componentes refletem no browser sem refresh manual
- [ ] **Verificacao:** Modificar texto no `page.tsx`, confirmar atualizacao automatica

### AC9: Variaveis de ambiente configuradas
- [x] `.env.local` com todas as variaveis necessarias (gitignored)
- [x] `.env.example` documentado com TODAS as variaveis e comentarios:
  ```
  # Supabase
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  SUPABASE_SERVICE_ROLE_KEY=
  
  # Claude API
  ANTHROPIC_API_KEY=
  
  # Asaas
  ASAAS_API_KEY=
  ASAAS_WEBHOOK_SECRET=
  
  # App
  NEXT_PUBLIC_APP_URL=
  ```
- [x] Regra: apenas `NEXT_PUBLIC_*` expostas ao client
- [x] **Verificacao:** `.env.example` existe e esta comitado; `.env.local` esta no `.gitignore`

### AC10: Supabase clients configurados
- [x] `src/lib/supabase/client.ts` — Browser client (singleton, usa anon key + cookies)
- [x] `src/lib/supabase/server.ts` — Server client (usa service role key, para API routes)
- [x] `src/lib/supabase/middleware.ts` — Helper para verificacao de sessao no middleware
- [x] Tipagem: `src/lib/supabase/types.ts` (placeholder para `supabase gen types`)
- [ ] **Verificacao:** Health check usa server client com sucesso; sem erros de tipagem

### AC11: shadcn/ui inicializado
- [x] `npx shadcn@latest init` executado
- [x] `components.json` configurado (estilo, cor base, CSS variables)
- [x] `tailwind.config.ts` com theme estendido (paleta financeira: tons azul/verde escuro)
- [x] `src/app/globals.css` com Tailwind directives + CSS custom properties do shadcn
- [x] Pelo menos 1 componente shadcn instalado para validar setup (ex: Button)
- [ ] **Verificacao:** Componente Button renderiza corretamente na pagina placeholder

### AC12: PWA manifest basico
- [x] `public/manifest.json` conforme architecture.md section 7.4
- [x] Referencia no `layout.tsx` via metadata.manifest
- [x] Placeholder para icones em `public/icons/`
- [ ] **Verificacao:** Chrome DevTools > Application > Manifest mostra dados corretos

---

## Dependencies

- **Blockers:** Nenhum — esta e a primeira story
- **Blocks:** Story 1.2, 1.3, 1.4 (todas dependem da fundacao)

---

## Tech Notes for @dev

### Supabase Setup
1. Criar projeto em `app.supabase.com` (free tier)
2. Instalar Supabase CLI: `npm install -g supabase`
3. `supabase init` na raiz do projeto
4. Copiar TODAS as 8 tabelas + indexes + constraints + functions + triggers + RLS do `docs/architecture/architecture.md` section 3 para `supabase/migrations/001_initial_schema.sql`
5. `supabase db push` para aplicar

### Supabase Clients
- **Browser client** (`client.ts`): `createBrowserClient()` do `@supabase/ssr` — funciona com cookies do Next.js, respeita RLS
- **Server client** (`server.ts`): `createServerClient()` com service role key — bypassa RLS, so usar em API routes
- Instalar: `@supabase/supabase-js` + `@supabase/ssr`

### shadcn/ui
- Seguir wizard: `npx shadcn@latest init`
- Paleta: tons azul escuro/verde que transmitam confianca financeira (nao gamificado)
- Configurar CSS variables para dark mode futuro

### Estrutura de pastas minima
```
src/
  app/
    (auth)/        # Grupo de rotas publicas (login/signup) — criar pasta vazia
    (app)/         # Grupo de rotas protegidas — criar pasta vazia  
    api/
      health/
        route.ts   # Health check
    layout.tsx
    page.tsx
    globals.css
  components/
    ui/            # shadcn/ui components
  lib/
    supabase/
      client.ts
      server.ts
      middleware.ts
      types.ts
    utils/
      constants.ts
  hooks/
```

### Logging
- Dev: `console.log/warn/error` (suficiente para MVP)
- Prod: considerar logs estruturados JSON (Vercel captura stdout)
- Sentry: sera adicionado depois, nao e obrigatorio nesta story

---

## Out of Scope

- Paginas de login/signup (Story 1.2)
- Importacao de arquivos (Story 1.3)
- Dashboard com dados (Story 1.4)
- Sentry/monitoring real (Epic 4)
- CI/CD automatizado (basico via Vercel auto-deploy, configuracao avancada no Epic 4)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/app/layout.tsx` | Done | Root layout with PT-BR, Inter font, PWA meta |
| `src/app/page.tsx` | Done | Placeholder page with Finansim branding |
| `src/app/globals.css` | Done | Tailwind + shadcn CSS with financial palette |
| `src/app/api/health/route.ts` | Done | Health check endpoint (GET, public) |
| `src/middleware.ts` | Done | Auth middleware (session refresh) |
| `src/lib/supabase/client.ts` | Done | Browser Supabase client (SSR) |
| `src/lib/supabase/server.ts` | Done | Server + Service Role clients |
| `src/lib/supabase/middleware.ts` | Done | Auth session update helper |
| `src/lib/supabase/types.ts` | Done | Manual DB types (placeholder for gen) |
| `src/lib/utils/constants.ts` | Done | App constants, categories, plans |
| `src/components/ui/button.tsx` | Done | shadcn/ui Button component |
| `src/lib/utils.ts` | Done | shadcn cn() utility |
| `supabase/migrations/001_initial_schema.sql` | Done | Full DB schema (8 tables, RLS, triggers) |
| `supabase/config.toml` | Done | Supabase local config |
| `.env.example` | Done | Env vars template (documented) |
| `.env.local` | Done | Local env with Supabase credentials |
| `public/manifest.json` | Done | PWA manifest |
| `public/icons/.gitkeep` | Done | Placeholder for PWA icons |
| `components.json` | Done | shadcn/ui config |
| `next.config.ts` | Done | Next.js config |
| `.gitignore` | Done | Git ignore rules (Next.js + Supabase added) |
| `.prettierrc` | Done | Prettier config |
| `.prettierignore` | Done | Prettier ignore patterns |
| `eslint.config.mjs` | Done | ESLint + Prettier config |
| `package.json` | Done | Dependencies + scripts (lint, format) |
