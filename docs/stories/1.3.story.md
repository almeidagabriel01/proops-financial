# Story 1.3: OFX/CSV Import & Transaction Parsing

**Epic:** 1 — Foundation & Core Infrastructure
**Status:** InProgress
**Complexity:** L (Large) — 8 Story Points
**Estimated Effort:** 4-5 dias
**Author:** River (@sm)
**PRD Ref:** Story 1.3 (prd.md v1.3), FR1-FR5
**Arch Ref:** Sections 2, 3, 4.1, 4.2, 11.1 (architecture.md v1.2)

---

## Context

Esta e a story mais complexa do Epic 1. O fluxo de importacao e o core do produto — sem ele, nao ha dados para dashboard, categorizacao ou chat. O pipeline foi dividido em duas fases pelo QA review (B1 — BLOCKER):

- **Phase 1 (Sync):** Upload + parse + dedup + insert. Roda em API Route do Vercel com limite de 10s (Hobby tier). Nao inclui categorizacao.
- **Phase 2 (Async):** Categorizacao via Supabase Edge Function. Roda em Deno runtime (sem limite de tempo do Vercel). Notifica o frontend via Supabase Realtime quando completa.

Nesta story, implementamos apenas a **Phase 1 completa** + o **trigger para Phase 2** + a **UI de importacao com Realtime**. A logica de categorizacao real (IA) sera implementada no Epic 2, Story 2.1. Aqui, a Edge Function e um stub que marca todas as transacoes como `category='outros'`, `category_source='ai'` e muda o status para `completed`.

**QA Note m7:** Supabase Edge Functions rodam em **Deno runtime**, nao Node.js. Imports usam URLs ou `npm:` prefix, nao `require()`.
**QA Note m8:** Sequencia de status do import: `processing` → `categorizing` → `completed` (ou `failed`). O frontend deve observar essa progressao via Realtime.

**PRD refs:** FR1 (import OFX/CSV), FR2 (parsing), FR4 (dedup), FR5 (historico)
**Arch refs:** Import Pipeline (section 4.2), Storage (section 3.3), DataSource abstraction (section 11.1)

---

## User Story

> Como usuario,
> quero importar o extrato do meu banco em formato OFX ou CSV,
> para que o app tenha acesso as minhas transacoes sem precisar digitar nada.

---

## Acceptance Criteria

### AC1: API route de upload
- [x] `src/app/api/import/route.ts` — POST, autenticado
- [x] Aceita `FormData` com: `file` (arquivo OFX ou CSV, max 5MB) e `bank_name` (string)
- [x] Validacoes:
  - Auth: verificar sessao Supabase (server client)
  - Tipo: apenas `.ofx` e `.csv` (rejeitar outros com 400)
  - Tamanho: max 5MB (rejeitar com 413)
- [x] Upload do arquivo original para Supabase Storage: `imports/{user_id}/{import_id}.{ext}`
- [x] Retorna JSON: `{ importId, transactionCount, duplicatesSkipped, status: 'categorizing' }`
- [ ] **Verificacao:** Upload de arquivo OFX valido retorna 200 com contagem; arquivo invalido retorna 400

### AC2: Parser de OFX
- [x] `src/lib/parsers/ofx-parser.ts` implementado
- [x] Extrai de cada transacao: `date`, `amount`, `description`, `type` (credit/debit), `external_id` (FITID do OFX)
- [x] Usa parsing manual do formato SGML (sem dependencia externa)
- [x] Amount: positivo = credito, negativo = debito (conforme schema)
- [ ] **Verificacao:** Parser processa arquivo OFX de exemplo e retorna `ParsedTransaction[]` correto

### AC3: Parser de CSV
- [x] `src/lib/parsers/csv-parser.ts` implementado
- [x] Deteccao automatica de delimitador (`,` ou `;`)
- [x] `src/lib/parsers/bank-formats/` com mapeamento de colunas por banco:
  - `nubank.ts` — formato Nubank CSV
  - `itau.ts` — formato Itau CSV
  - `bradesco.ts` — formato Bradesco CSV
  - `index.ts` — detector de formato (tenta cada banco, fallback para formato generico)
- [x] Cada format define: quais colunas sao date, description, amount e como parsear cada uma
- [x] Amount: normalizar para positivo (credito) / negativo (debito) independente do formato do banco
- [ ] **Verificacao:** Parser processa CSVs de exemplo de cada banco e retorna `ParsedTransaction[]` correto

### AC4: Tipo ParsedTransaction
- [x] `src/lib/parsers/types.ts` com interface:
  ```typescript
  interface ParsedTransaction {
    external_id: string;  // FITID or SHA-256 hash
    date: string;         // ISO date (YYYY-MM-DD)
    description: string;  // Raw bank description
    amount: number;       // Positive=credit, negative=debit
    type: 'credit' | 'debit';
  }
  ```
- [x] Interface `DataSource` (placeholder para Open Finance futuro):
  ```typescript
  interface DataSource {
    type: 'file' | 'open_finance';
    fetchTransactions(params: FetchParams): Promise<ParsedTransaction[]>;
  }
  ```
- [x] **Verificacao:** Tipos compilam sem erros; usados consistentemente nos parsers

### AC5: Deduplicacao
- [x] OFX: usar FITID nativo do banco como `external_id`
- [x] CSV: gerar `external_id` via SHA-256 hash de `date + amount + normalize_description(description)`
  - Funcao `normalize_description()`: lowercase + trim + remove acentos + remove caracteres especiais (mesma logica da funcao SQL no banco)
  - Implementado em `src/lib/parsers/utils.ts`
- [x] Antes de inserir: verificar `external_id` contra transacoes existentes do mesmo `bank_account_id`
- [x] Duplicatas sao silenciosamente ignoradas (contadas em `duplicates_skipped`)
- [x] `transactions.external_id` e NOT NULL com constraint UNIQUE(user_id, bank_account_id, external_id)
- [ ] **Verificacao:** Importar mesmo arquivo 2x; segunda importacao reporta 0 novas transacoes e N duplicatas

### AC6: Insercao de transacoes
- [x] Criar ou reutilizar `bank_account` baseado no `bank_name`
- [x] Criar registro em `imports` com `status='processing'`
- [x] Inserir transacoes com:
  - `category = 'outros'` (placeholder — categorizacao no Epic 2)
  - `category_source = 'pending'`
  - `category_confidence = null`
- [x] Atualizar `imports`: `status = 'categorizing'`, `transaction_count`, `duplicates_skipped`
- [x] Atualizar `bank_accounts.last_import_at`
- [ ] **Verificacao:** Apos import, tabelas `imports`, `transactions`, `bank_accounts` preenchidas corretamente

### AC7: Trigger para Edge Function (async)
- [x] Apos insercao: invocar Supabase Edge Function `categorize-import` com `{ importId, userId }`
- [x] Invocacao via `supabase.functions.invoke('categorize-import', { body: { importId, userId } })`
- [x] Se a invocacao falhar: logar erro, mas NAO falhar o import (transacoes ja estao salvas com `category='outros'`)
- [ ] **Verificacao:** Apos import, Edge Function e chamada (verificar nos logs do Supabase)

### AC8: Edge Function stub (categorize-import)
- [x] `supabase/functions/categorize-import/index.ts` implementada em **Deno runtime**
- [x] **QA Note m7:** Usa `npm:@supabase/supabase-js@2` prefix (Deno-style). NAO usa `require()`.
- [x] Stub behavior (categorizacao real no Epic 2):
  1. Receber `{ importId, userId }`
  2. Marcar todas as transacoes do import como `category='outros'`, `category_source='ai'`
  3. Atualizar `imports.status = 'completed'`
- [x] **QA Note m8:** Sequencia de status: `processing` → `categorizing` (set pela API route) → `completed` (set pela Edge Function)
- [ ] **Verificacao:** Apos import, Edge Function executa e status do import muda para `completed`

### AC9: Pagina de importacao (UI)
- [x] `src/app/(app)/import/page.tsx` — Client Component
- [x] `src/components/import/file-dropzone.tsx` — Area de drag-and-drop + botao "Selecionar arquivo"
  - Aceita apenas .ofx e .csv
  - Exibe nome e tamanho do arquivo selecionado
  - Feedback visual de drag-over
- [x] `src/components/import/import-progress.tsx` — Componente de progresso:
  - Estado 1: "Enviando arquivo..." (durante upload)
  - Estado 2: "Processando extrato..." (status = 'processing')
  - Estado 3: "Categorizando transacoes..." com spinner (status = 'categorizing')
  - Estado 4: "Pronto! X transacoes importadas, Y duplicatas ignoradas" (status = 'completed')
  - Estado 5: "Erro ao processar arquivo" com mensagem (status = 'failed')
- [x] Selector de banco (dropdown com Nubank, Itau, Bradesco, Outro)
- [x] Botao "Importar" desabilitado ate arquivo ser selecionado
- [ ] **Verificacao:** Fluxo completo funciona visualmente no mobile

### AC10: Supabase Realtime para status
- [x] Frontend subscribe no canal Realtime para a tabela `imports` filtrado pelo `importId`
- [x] Quando `status` muda: UI atualiza automaticamente (categorizing → completed)
- [x] Unsubscribe ao sair da pagina (cleanup no useEffect)
- [ ] **Verificacao:** Abrir import page; importar arquivo; ver transicao automatica processing → categorizing → completed sem refresh

### AC11: Testes unitarios dos parsers
- [x] `tests/unit/parsers/ofx-parser.test.ts` com arquivo OFX de exemplo
- [x] `tests/unit/parsers/csv-parser.test.ts` com CSVs de exemplo (Nubank, Itau, Bradesco)
- [x] `tests/fixtures/` com arquivos de exemplo:
  - `itau-sample.ofx` — extrato OFX real (anonimizado)
  - `nubank-sample.csv` — CSV Nubank
  - `bradesco-sample.csv` — CSV Bradesco
- [x] Testar: parsing correto, amount positivo/negativo, external_id gerado, deteccao de formato
- [x] **Verificacao:** `npm test` passa todos os 29 testes

### AC12: Feedback de erro
- [x] Arquivo invalido (nao OFX/CSV): "Formato nao suportado. Envie um arquivo .ofx ou .csv"
- [x] Arquivo muito grande: "Arquivo muito grande. Maximo permitido: 5MB"
- [x] Parsing falha: "Nao foi possivel processar este arquivo. Verifique se e um extrato bancario valido"
- [x] Erro de rede: "Erro de conexao. Tente novamente"
- [x] Mensagens em PT-BR, exibidas na UI (nao console)
- [ ] **Verificacao:** Cada cenario de erro exibe mensagem correta

---

## Dependencies

- **Blocked by:** Story 1.1 (projeto, schema, Supabase), Story 1.2 (auth — import precisa de usuario autenticado)
- **Blocks:** Story 1.4 (dashboard precisa de transacoes importadas para exibir dados reais)

---

## Tech Notes for @dev

### Pipeline Flow (resumo visual)
```
Upload → Validate → Storage → Parse → Dedup → Insert → Status='categorizing' → Invoke Edge Function
                                                                                        │
                                                                              (async, Deno runtime)
                                                                                        │
                                                                            Stub: set category='outros'
                                                                            Status='completed'
                                                                            Realtime → UI update
```

### Supabase Edge Function (Deno)
```typescript
// supabase/functions/categorize-import/index.ts
// DENO RUNTIME — nao Node.js!

import { createClient } from 'npm:@supabase/supabase-js@2'

Deno.serve(async (req) => {
  const { importId, userId } = await req.json()
  
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  )
  
  // Stub: marcar todas como 'outros' (categorizacao real no Epic 2)
  await supabase
    .from('transactions')
    .update({ category: 'outros', category_source: 'ai' })
    .eq('import_id', importId)
    .eq('category_source', 'pending')
  
  // Atualizar status do import
  await supabase
    .from('imports')
    .update({ status: 'completed' })
    .eq('id', importId)
  
  return new Response(JSON.stringify({ success: true }), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

### SHA-256 para CSV dedup
```typescript
// src/lib/parsers/utils.ts
import { createHash } from 'crypto'

export function normalizeDescription(raw: string): string {
  return raw
    .toLowerCase()
    .trim()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '') // remove accents
    .replace(/[^a-z0-9 ]/g, '')     // remove special chars
    .replace(/\s+/g, ' ')           // collapse whitespace
}

export function generateExternalId(date: string, amount: number, description: string): string {
  const normalized = normalizeDescription(description)
  const input = `${date}|${amount}|${normalized}`
  return createHash('sha256').update(input).digest('hex')
}
```

### Supabase Realtime (client-side)
```typescript
// Na pagina de import, apos receber o importId:
const channel = supabase
  .channel(`import-${importId}`)
  .on('postgres_changes', {
    event: 'UPDATE',
    schema: 'public',
    table: 'imports',
    filter: `id=eq.${importId}`
  }, (payload) => {
    setImportStatus(payload.new.status)
  })
  .subscribe()

// Cleanup:
return () => { supabase.removeChannel(channel) }
```

### OFX Parser
- Considerar `ofx-js` (npm) ou parsing manual (OFX e SGML-like, nao XML)
- Formato tipico: `<STMTTRN>` blocks com `<DTPOSTED>`, `<TRNAMT>`, `<NAME>`, `<FITID>`
- FITID e o identificador unico da transacao (usado como external_id)

### CSV Bank Formats
- **Nubank:** `Data,Valor,Descrição` (sem header no mais recente, ou com header)
- **Itau:** separador `;`, formato de data `DD/MM/YYYY`, valor com virgula decimal
- **Bradesco:** similar ao Itau mas layout de colunas diferente
- Cada banco precisa de um adaptador que mapeia colunas para `ParsedTransaction`

### Supabase Storage RLS
- Path: `imports/{user_id}/{import_id}.{ext}`
- RLS ja configurada na Story 1.1: usuario so acessa arquivos no seu folder

### Free vs Premium (import)
- Free: 1 bank_account apenas (CON no futuro — nao precisa implementar controle agora)
- Para MVP: permitir multiplos por enquanto, controle sera adicionado no Epic 3

---

## Out of Scope

- Categorizacao real com IA (Epic 2, Story 2.1 — aqui e stub `category='outros'`)
- Controle de limite de bank_accounts por plano (Epic 3)
- Formatos de banco alem de Nubank, Itau, Bradesco (extensivel via `bank-formats/`)
- Re-importacao / sync automatico (futuro com Open Finance)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/app/api/import/route.ts` | ✅ Created | Import API route (sync phase) |
| `src/app/(app)/import/page.tsx` | ✅ Created | Import page (Client Component) |
| `src/components/import/file-dropzone.tsx` | ✅ Created | File drag-and-drop component |
| `src/components/import/import-progress.tsx` | ✅ Created | Import progress tracker |
| `src/lib/parsers/ofx-parser.ts` | ✅ Created | OFX file parser (manual SGML) |
| `src/lib/parsers/csv-parser.ts` | ✅ Created | CSV file parser |
| `src/lib/parsers/types.ts` | ✅ Created | ParsedTransaction + DataSource interfaces |
| `src/lib/parsers/utils.ts` | ✅ Created | normalizeDescription + generateExternalId |
| `src/lib/parsers/bank-formats/nubank.ts` | ✅ Created | Nubank CSV format |
| `src/lib/parsers/bank-formats/itau.ts` | ✅ Created | Itau CSV format |
| `src/lib/parsers/bank-formats/bradesco.ts` | ✅ Created | Bradesco CSV format |
| `src/lib/parsers/bank-formats/index.ts` | ✅ Created | Format auto-detector |
| `supabase/functions/categorize-import/index.ts` | ✅ Created | Edge Function stub (Deno) |
| `tests/unit/parsers/ofx-parser.test.ts` | ✅ Created | OFX parser tests (8 tests) |
| `tests/unit/parsers/csv-parser.test.ts` | ✅ Created | CSV parser tests (21 tests) |
| `tests/fixtures/itau-sample.ofx` | ✅ Created | OFX test fixture |
| `tests/fixtures/nubank-sample.csv` | ✅ Created | Nubank CSV fixture |
| `tests/fixtures/bradesco-sample.csv` | ✅ Created | Bradesco CSV fixture |
| `vitest.config.ts` | ✅ Created | Vitest configuration |
| `src/lib/supabase/types.ts` | ✅ Updated | Added Relationships[] to all tables (supabase-js v2 compat) |
| `tsconfig.json` | ✅ Updated | Exclude supabase/functions (Deno runtime) |
| `package.json` | ✅ Updated | Added test + test:watch scripts |
