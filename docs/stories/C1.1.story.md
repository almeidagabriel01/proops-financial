# Story C1.1: Notas em Transações

**Epic:** C1 — Ciclo 1: MVP Extensions
**Status:** Draft
**Complexity:** S (Small) — 1 Story Point
**Estimated Effort:** 1 dia
**Author:** River (@sm)
**PRD Ref:** docs/market-research-features.md (Ciclo 1, "Notas em transações")
**Arch Ref:** docs/feature-inventory.md ("Campos ausentes: notes")

---

## Context

A tabela `transactions` não possui campo `notes`. Usuários não têm onde registrar contexto adicional sobre uma transação — por exemplo, "reembolso da empresa", "presente de aniversário da Maria", ou "parcela do conserto do carro".

Todos os concorrentes sérios (Copilot Money, Monarch Money, YNAB, Mobills) oferecem notas em transações. É uma das features mais simples de implementar e elimina uma reclamação direta: o usuário precisa lembrar o contexto de uma transação semanas depois.

A PATCH `PATCH /api/transactions/[id]` já existe e tem padrão extensível — basta adicionar `notes` ao `UpdateTransactionBody` e ao `updates` object. O `TransactionDetail` sheet já existe em `src/components/transactions/transaction-detail.tsx`.

---

## User Story

> Como usuário,
> quero adicionar uma nota livre em qualquer transação,
> para registrar contexto adicional (reembolso, presente, motivo específico)
> e encontrá-lo quando consultar meu histórico financeiro.

---

## Acceptance Criteria

### AC1: Migration — campo `notes` na tabela `transactions`

- [ ] Nova migration `014_transaction_notes.sql` em `supabase/migrations/`
- [ ] Adiciona coluna: `ALTER TABLE transactions ADD COLUMN notes text;`
- [ ] Coluna é nullable (sem valor padrão) — compatível com transações existentes
- [ ] **Verificação:** `supabase db push` sem erros; `SELECT notes FROM transactions LIMIT 1` retorna null para transações existentes

### AC2: API — PATCH /api/transactions/[id] aceita campo `notes`

- [ ] `UpdateTransactionBody` em `src/app/api/transactions/[id]/route.ts` inclui `notes?: string | null`
- [ ] Se `body.notes !== undefined`: adicionar ao `updates` object
- [ ] Validação: `notes` com mais de 500 caracteres retorna 400 com mensagem `'Nota muito longa (máximo 500 caracteres)'`
- [ ] `notes: null` limpa a nota (permite apagar)
- [ ] A coluna `notes` aparece no `select()` do PATCH (já retorna a transação completa)
- [ ] **Verificação:** `PATCH /api/transactions/{id}` com `{ "notes": "reembolso empresa" }` → 200 com `notes` no body de resposta

### AC3: UI — campo de nota na TransactionDetail sheet

- [ ] `src/components/transactions/transaction-detail.tsx` exibe campo de texto abaixo da seção de categoria
- [ ] Label: "Nota" (ou "Adicionar nota" quando vazia)
- [ ] Textarea com `maxLength={500}` e placeholder `"Ex: reembolso, presente, motivo específico..."`
- [ ] Comportamento: edição inline — ao perder foco (onBlur) dispara PATCH se valor mudou
- [ ] Estado de salvamento: ícone de loading durante PATCH, checkmark por 1s após sucesso
- [ ] Campo exibido para todos os usuários (Basic e Pro)
- [ ] **Verificação:** Abrir detalhe de qualquer transação → campo de nota visível; digitar e sair do campo → nota salva e persiste ao reabrir o sheet

### AC4: Exibição na lista de transações

- [ ] `src/components/transactions/transaction-item.tsx` exibe ícone de nota (📝 ou ícone shadcn `FileText`) quando `notes` não é null/vazio
- [ ] Ao exibir a nota na lista: truncada em 60 caracteres com reticências
- [ ] Nota completa visível no TransactionDetail sheet (AC3)
- [ ] **Verificação:** Transação com nota → ícone visível na lista; ao abrir detalhe → nota completa exibida

---

## Dependencies

- **Blocked by:** Nenhuma
- **Blocks:** Nenhuma

---

## Tech Notes for @dev

### Migration

```sql
-- supabase/migrations/014_transaction_notes.sql
ALTER TABLE transactions ADD COLUMN notes text;
```

Sem NOT NULL, sem default — retrocompatível com todas as transações existentes.

### Extensão da PATCH route

```typescript
// src/app/api/transactions/[id]/route.ts
interface UpdateTransactionBody {
  date?: string;
  description?: string;
  amount?: number;
  type?: 'credit' | 'debit';
  category?: string;
  notes?: string | null;  // NOVO
}

// No bloco de validações:
if (body.notes !== undefined) {
  if (body.notes !== null && body.notes.length > 500) {
    return Response.json({ error: 'Nota muito longa (máximo 500 caracteres)' }, { status: 400 });
  }
  updates.notes = body.notes; // null limpa, string salva
}
```

### TransactionDetail — edição inline com onBlur

```typescript
// Salvar apenas quando o valor mudou
const [noteValue, setNoteValue] = useState(transaction.notes ?? '');
const [savedNote, setSavedNote] = useState(transaction.notes ?? '');

async function handleNoteBlur() {
  if (noteValue === savedNote) return; // sem mudança
  // PATCH e atualizar savedNote
}
```

### Tipagem

Regenerar os tipos após a migration:
```bash
supabase gen types typescript --local > src/lib/supabase/types.ts
```

O campo `notes: string | null` deve aparecer automaticamente no tipo `Database['public']['Tables']['transactions']['Row']`.

---

## Out of Scope

- Busca full-text dentro das notas
- Notas com formatação (markdown, negrito)
- Histórico de edições da nota
- Notas em transações agendadas (`scheduled_transactions`)
- Exportação de notas no CSV/JSON

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `supabase/migrations/014_transaction_notes.sql` | Created | ADD COLUMN notes text |
| `src/lib/supabase/types.ts` | Updated | Regenerado após migration |
| `src/app/api/transactions/[id]/route.ts` | Updated | PATCH aceita campo notes |
| `src/components/transactions/transaction-detail.tsx` | Updated | Campo textarea de nota com onBlur save |
| `src/components/transactions/transaction-item.tsx` | Updated | Ícone de nota quando notas != null |

---

## QA Results

### Revisão Pré-Implementação — 2026-04-10
**Agente:** Quinn (@qa)
**Verdict:** PASS ✅
**Gate:** 0 BLOCKERs · 0 MAJORs · 2 MINORs

**Autorizado para desenvolvimento.** ✅

#### MINORs

**mn1 — AC4 ambíguo: ícone vs. texto truncado na lista**
AC4 menciona "exibe ícone de nota" E "truncada em 60 caracteres" como itens separados. Não está claro se o design final é: (a) apenas ícone na lista + texto completo no detalhe, ou (b) ícone + texto truncado visível na lista. @dev deve escolher e documentar — recomendado (a) para manter a `TransactionItem` compacta em mobile.

**mn2 — Sem AC para estado de erro do onBlur save**
AC3 especifica loading + checkmark de sucesso, mas não feedback de falha. Se o PATCH falhar (timeout, rede), o usuário não tem indicação visual. Adicionar: ao falhar, restaurar o valor original e exibir toast de erro breve ("Erro ao salvar nota — tente novamente").

---

## Dev Agent Record

### Agent Model Used
_A preencher pelo @dev_

### Completion Notes
_A preencher pelo @dev_

### Change Log
| Date | Agent | Change |
|------|-------|--------|
| 2026-04-10 | River (@sm) | Story criada para Epic C1 |
