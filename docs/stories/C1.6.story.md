# Story C1.6: Detecção de Sazonalidades Brasileiras

**Epic:** C1 — Ciclo 1: MVP Extensions
**Status:** Draft
**Complexity:** S (Small) — 2 Story Points
**Estimated Effort:** 2 dias
**Author:** River (@sm)
**PRD Ref:** docs/market-research-features.md (Ciclo 1, "Detecção de sazonalidades BR")
**Arch Ref:** docs/feature-inventory.md ("Detecção de sazonalidades BR: ⬜ não existe")

---

## Context

Nenhum concorrente brasileiro (nem internacional) implementa bem a detecção de sazonalidades fiscais e culturais do Brasil. É um "white space" de mercado identificado pela análise competitiva.

O contexto brasileiro tem gastos sazonais previsíveis e significativos: IPVA em janeiro, IPTU em fevereiro, material escolar em fevereiro, imposto de renda em março-abril, Black Friday em novembro, compras de natal em dezembro. Usuários são pegos de surpresa todos os anos — mesmo após o primeiro ano no app, não têm visibilidade de quanto gastaram naquele período.

A feature é contextual: aparece no mês relevante, usa o histórico do usuário para estimar o valor esperado, e orienta o usuário a se preparar. Sem nova tabela — lógica Server Component no dashboard.

---

## User Story

> Como usuário,
> quero ver alertas contextuais no dashboard sobre gastos sazonais previsíveis do calendário brasileiro,
> para me preparar financeiramente antes que cheguem IPVA, IPTU, material escolar e outras despesas sazonais,
> baseado no que eu mesmo gastei no mesmo período do ano anterior.

---

## Acceptance Criteria

### AC1: Mapeamento de sazonalidades BR por mês

- [ ] Implementar constante `BRAZIL_SEASONALITIES` em `src/lib/dashboard/seasonalities.ts`
- [ ] Mapeamento completo:
  | Mês | Keywords | Label |
  |-----|----------|-------|
  | Janeiro (1) | ipva, licenciamento, veículo, crlv | "IPVA e licenciamento do veículo" |
  | Fevereiro (2) | iptu, material escolar, matrícula, uniforme | "IPTU e material escolar" |
  | Março–Abril (3-4) | irpf, imposto renda, receita federal, ir | "Declaração do Imposto de Renda" |
  | Junho–Julho (6-7) | viagem, passagem, hotel, hospedagem, férias | "Temporada de férias e viagens" |
  | Novembro (11) | black friday, blackfriday | "Black Friday" |
  | Dezembro (12) | natal, presente, confraternização, amigo secreto | "Gastos de fim de ano" |

- [ ] Busca de keywords é case-insensitive e compara contra `description` normalizada
- [ ] **Verificação:** Testes unitários cobrem detecção correta por mês e keyword

### AC2: Cálculo do gasto estimado baseado no ano anterior

- [ ] Função `getSeasonalityEstimate(supabase, userId, month, keywords)` em `src/lib/dashboard/seasonalities.ts`
- [ ] Busca transações do usuário no **mesmo mês do ano anterior** cujas descrições contêm pelo menos uma das keywords
- [ ] Retorna: `{ total: number, transactionCount: number, yearMonth: string }`
- [ ] Se não há histórico (primeiro uso do app): retorna `null` — sem estimativa
- [ ] **Verificação:** Usuário com transações de IPVA em janeiro/2025 → função retorna o total gasto

### AC3: Função para sazonalidades ativas no mês corrente

- [ ] Função `getActiveSeasonalities(month: number): SeasonalityInfo[]` em `src/lib/dashboard/seasonalities.ts`
- [ ] Retorna apenas as sazonalidades ativas para o mês informado
- [ ] `SeasonalityInfo`: `{ label: string, keywords: string[], icon: string, month: number }`
- [ ] Meses de dois dígitos (ex: 3-4 para IRPF) devem ser representados como array de meses no mapeamento
- [ ] **Verificação:** `getActiveSeasonalities(2)` retorna IPTU + material escolar; `getActiveSeasonalities(7)` retorna férias; `getActiveSeasonalities(5)` retorna `[]`

### AC4: Card de sazonalidade no dashboard

- [ ] Novo componente `SeasonalityCard` em `src/components/dashboard/seasonality-card.tsx`
- [ ] Exibido no dashboard **somente no mês relevante** e **somente quando há sazonalidades ativas**
- [ ] Posicionado após `DuplicateAlertsCard` (C1.5) e antes dos gráficos
- [ ] Visual: card com borda azul/informativo, ícone de calendário
- [ ] Conteúdo por sazonalidade ativa:
  - Título: "Em [mês], chegam [label]"
  - Se há histórico: "No ano passado você gastou **R$ X,XX** nesta categoria (N transações)"
  - Se não há histórico: "Verifique seus gastos com [label] para se preparar"
  - CTA: link para `/transactions?month=[ano-passado-mesmo-mês]&search=[primeira-keyword]`
- [ ] Se múltiplas sazonalidades no mesmo mês: exibir como lista dentro do mesmo card
- [ ] **Verificação:** Testar em fevereiro → card mostra IPTU + material escolar com estimativa; em março → card mostra IRPF; em junho/julho → férias; em agosto → sem card

### AC5: Integração no dashboard (Server Component)

- [ ] `src/app/(app)/dashboard/page.tsx` chama `getActiveSeasonalities()` e, para cada sazonalidade ativa, `getSeasonalityEstimate()`
- [ ] Dados passados como props ao `SeasonalityCard`
- [ ] Zero impacto de performance: as queries de sazonalidade são simples aggregations com filtro de mês — adicionar ao fetch paralelo existente
- [ ] **Verificação:** Dashboard em mês sem sazonalidade → sem card; dashboard em janeiro → card de IPVA presente

---

## Dependencies

- **Blocked by:** Nenhuma
- **Blocks:** Nenhuma

---

## Tech Notes for @dev

### Estrutura do mapeamento

```typescript
// src/lib/dashboard/seasonalities.ts

interface SeasonalityConfig {
  label: string;
  keywords: string[];
  icon: string; // nome do ícone Lucide
  months: number[]; // 1=jan ... 12=dez
}

export const BRAZIL_SEASONALITIES: SeasonalityConfig[] = [
  {
    label: 'IPVA e licenciamento do veículo',
    keywords: ['ipva', 'licenciamento', 'veículo', 'crlv', 'detran'],
    icon: 'Car',
    months: [1],
  },
  {
    label: 'IPTU e material escolar',
    keywords: ['iptu', 'material escolar', 'matrícula', 'uniforme', 'mochila'],
    icon: 'School',
    months: [2],
  },
  {
    label: 'Declaração do Imposto de Renda',
    keywords: ['irpf', 'imposto de renda', 'receita federal', ' ir '],
    icon: 'FileText',
    months: [3, 4],
  },
  {
    label: 'Temporada de férias e viagens',
    keywords: ['viagem', 'passagem', 'hotel', 'hospedagem', 'férias', 'airbnb'],
    icon: 'Plane',
    months: [6, 7],
  },
  {
    label: 'Black Friday',
    keywords: ['black friday', 'blackfriday', 'black'],
    icon: 'ShoppingBag',
    months: [11],
  },
  {
    label: 'Gastos de fim de ano',
    keywords: ['natal', 'presente', 'confraternização', 'amigo secreto', 'ceia'],
    icon: 'Gift',
    months: [12],
  },
];
```

### Query para estimativa do ano anterior

```typescript
export async function getSeasonalityEstimate(
  supabase: SupabaseClient,
  userId: string,
  months: number[],
  keywords: string[]
): Promise<{ total: number; transactionCount: number } | null> {
  const lastYear = new Date().getFullYear() - 1;

  // Construir filtro de meses (ex: Janeiro do ano passado)
  const monthFilters = months.map(m =>
    `and(date.gte.${lastYear}-${String(m).padStart(2,'0')}-01,date.lt.${lastYear}-${String(m === 12 ? 1 : m+1).padStart(2,'0')}-01)`
  ).join(',');

  // Buscar transações com qualquer keyword nas descrições
  // Usar ilike com OR para múltiplas keywords
  let query = supabase
    .from('transactions')
    .select('amount')
    .eq('user_id', userId)
    .eq('type', 'debit');

  // Aplicar filtro de meses
  // Simplificação: buscar todas do ano passado nos meses corretos
  query = query
    .gte('date', `${lastYear}-01-01`)
    .lte('date', `${lastYear}-12-31`);

  const { data } = await query;

  // Filtrar client-side por keyword e mês (mais simples que SQL complexo)
  const matching = (data ?? []).filter(t => {
    const d = new Date(t.date);
    const txMonth = d.getMonth() + 1;
    if (!months.includes(txMonth)) return false;
    const desc = t.description.toLowerCase();
    return keywords.some(kw => desc.includes(kw.toLowerCase()));
  });

  if (matching.length === 0) return null;

  return {
    total: matching.reduce((sum, t) => sum + t.amount, 0),
    transactionCount: matching.length,
  };
}
```

> **Nota para @dev:** Se o volume de transações for alto (usuários com muitos dados), otimizar com uma query SQL direta usando `OR` de `ilike`. Para o MVP, a abordagem client-side é suficiente.

### Exibição condicional no dashboard

```typescript
// dashboard/page.tsx
const currentMonth = new Date().getMonth() + 1;
const activeSeasonalities = getActiveSeasonalities(currentMonth);

// Para cada sazonalidade ativa, buscar estimativa em paralelo
const estimates = await Promise.all(
  activeSeasonalities.map(s =>
    getSeasonalityEstimate(supabase, user.id, s.months, s.keywords)
  )
);
```

---

## Out of Scope

- Alertas push de sazonalidades
- Sazonalidades configuráveis pelo usuário
- 13º salário como "renda extra prevista" (seria feature diferente)
- Datas específicas de vencimento (ex: "IPTU de SP vence em março") — apenas contexto mensal
- Integração com calendário fiscal oficial da Receita Federal
- Sazonalidades por estado (ex: datas de IPVA variam por estado)

---

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/lib/dashboard/seasonalities.ts` | Created | Mapeamento BRAZIL_SEASONALITIES + funções getActiveSeasonalities + getSeasonalityEstimate |
| `src/components/dashboard/seasonality-card.tsx` | Created | Card condicional com contexto sazonal e estimativa |
| `src/app/(app)/dashboard/page.tsx` | Updated | Incluir SeasonalityCard quando mês tem sazonalidade ativa |
| `tests/unit/dashboard/seasonalities.test.ts` | Created | Testes unitários: detecção por mês, keywords, estimativa |

---

## QA Results

_A preencher pelo @qa_

---

## Dev Agent Record

### Agent Model Used
_A preencher pelo @dev_

### Completion Notes
_A preencher pelo @dev_

### Change Log
| Date | Agent | Change |
|------|-------|--------|
| 2026-04-10 | River (@sm) | Story criada para Epic C1 |
